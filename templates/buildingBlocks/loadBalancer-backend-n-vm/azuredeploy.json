{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "deploymentContext": {
      "type": "object",
      "defaultValue": {
        "parentTemplateUniqueString": "bbVM"
      }
    },
    "parameterFileLink": { "type": "string" },
    "protectedSettings": { "type": "secureObject" }
  },
  "variables": {
    "templateUniqueString": "[uniqueString(concat(string(parameters('deploymentContext')), parameters('parameterFileLink')))]",
    "parametersDeploymentName": "[concat(parameters('deploymentContext').parentTemplateUniqueString, '-params-', variables('templateUniqueString'))]",
    "parametersTemplate": "[uri(deployment().properties.templateLink.uri, 'parameterLink-resolver.json')]",
    "lbVMsDeploymentName": "[concat(parameters('deploymentContext').parentTemplateUniqueString, '-deployLBnVMs-', variables('templateUniqueString'))]",
    "lbVMsTemplate": "[uri(deployment().properties.templateLink.uri, 'loadBalancer-backend-n-vm-deploy.json')]"
  },
  "resources": [
    {
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2015-01-01",
      "name": "[variables('parametersDeploymentName')]",
      "properties": {
        "mode": "Incremental",
        "templateLink": {
          "uri": "[variables('parametersTemplate')]"
        },
        "parametersLink": {
          "uri": "[parameters('parameterFileLink')]"
        }
      }
    },
    {
      "type": "Microsoft.Resources/deployments",
      "apiVersion": "2015-01-01",
      "name": "[variables('lbVMsDeploymentName')]",
      "dependsOn": [
        "[variables('parametersDeploymentName')]"
      ],
      "properties": {
        "mode": "Incremental",
        "templateLink": {
          "uri": "[variables('lbVMsTemplate')]"
        },
        "parameters": {
          "deploymentContext": {
            "value": {
              "parentTemplateUniqueString": "[concat(variables('templateUniqueString'), '-deployLBnVMs')]"
            }
          },
          "loadBalancerSettings": {
            "value": "[reference(variables('parametersDeploymentName')).outputs.loadBalancerSettings.value]"
          },
          "virtualMachinesSettings": {
            "value": "[reference(variables('parametersDeploymentName')).outputs.virtualMachinesSettings.value]"
          },
          "virtualNetworkSettings": {
            "value": "[reference(variables('parametersDeploymentName')).outputs.virtualNetworkSettings.value]"
          },
          "buildingBlockSettings": {
            "value": "[reference(variables('parametersDeploymentName')).outputs.buildingBlockSettings.value]"
          },
          "protectedSettings": {
            "value": "[parameters('protectedSettings')]"
          }
        }
      }
    }
  ],
  "outputs": {
    "resources": {
      "type": "array",
      "value": "[reference(variables('lbVMsDeploymentName')).outputs.resources.value]"
    }
  }
}
